name: cron run
on:
  schedule:
    - cron: '25 3 * * *'

jobs:
  build:
    name: Trigger cron action
    runs-on: ubuntu-latest
    steps:
      - name: cURL request
        run: curl -X POST https://stock-price-prediction-ko1ao1f2h-hgoyal09.vercel.app/cron-functions/getRealtimeStockData
